# KururuCMS の依存パッケージ。
#
# 版を == で固定しているのは、「昨日は動いたのに今日は動かない」を無くすため。
# 上げるときは意図して上げ、テストを通してからコミットする。
#
# 追加した日:
#   1日目   Django, argon2-cffi
#   4日目   Pillow（アップロード画像の実体検証）
#   8日目   django-allauth（認証）, fido2 と qrcode は 9日目のパスキー/TOTP 用
#   10日目  psycopg, redis, gunicorn（本番構成）
Django==5.2.15
argon2-cffi==25.1.0
Pillow==12.3.0
# 角かっこは「追加機能（extras）」の指定。
# 必要な依存を allauth 自身に決めさせる。
#
#   mfa            … TOTP と パスキー（qrcode, fido2）
#   socialaccount  … Google / GitHub ログイン（requests, requests-oauthlib ほか）
#
# 9日目までは qrcode と fido2 を手で並べていた。
# それでも動いていたのは、開発機の仮想環境に requests が
# 別の経路で入っていたからで、宣言としては間違っていた。
# 手で書き写すと、上流が依存を増やしたときに気づけない。
django-allauth[mfa,socialaccount]==65.18.0

# --- 10日目 ---------------------------------------------------------------
# PostgreSQL ドライバ。psycopg2 ではなく psycopg（3系）を使う。
# [binary] はコンパイル済みの版で、ビルド環境が無くても入る。
psycopg[binary]==3.3.4

# Redis クライアント。Django 4.0 以降はキャッシュバックエンドが標準にあるので、
# django-redis のような追加パッケージは要らない。
redis==8.1.0

# 本番のアプリケーションサーバー。
# runserver は開発専用で、同時接続をさばく作りになっていない。
gunicorn==26.0.0
